=== TITLE ===
04 账户体系&资源体系 记录
=== VERSION ===
11
=== URL ===
https://wiki.sangfor.com/pages/viewpage.action?pageId=274119996
=== CONTENT ===
<p>应与公司的组织架构对应，但是组织架构可能分为水平、垂直、混合（矩阵）等。</p><table class="fixed-width wrapped" style="width: 1328.0px;"><colgroup><col style="width: 110.0px;" /><col style="width: 324.0px;" /><col style="width: 428.0px;" /><col style="width: 225.0px;" /><col style="width: 114.0px;" /><col style="width: 126.0px;" /></colgroup><tbody><tr><th scope="col"><br /></th><th scope="col">账户体系</th><th scope="col">权限体系</th><th scope="col">KB资源体系</th><th scope="col">优势</th><th scope="col">不足</th></tr><tr><td>Fastgpt</td><td><p>团队模式team、org、group</p><p><span style="color: rgb(51,51,51);">资源都是绑定到team的，有点类似openstack中的租户tenant。</span></p><ol><li><span style="color: rgb(51,51,51);">org有层级关系，<span style="color: rgb(97,97,97);">子成员自动继承父组织权限，可从企业微信等同步；</span></span></li><li><span style="color: rgb(51,51,51);">group扁平结构，用来简化权限配置，group资源有role，使用team_group_members管理 。</span></li></ol></td><td><ol><li>resource_permissions管理</li></ol><p>resourcetype: team、app、dataset、evaluation、model</p><p>verb：read、write、manage</p><p><br /></p><p>2. group</p></td><td><p>树形</p><p><br /></p><p>权限继承关系</p></td><td><p>用户层级关系</p></td><td><ol><li>没有role</li><li>fastgpt权限管理现有的问题是，在树形组织结构中，一般是越上面权限越大，但是采用继承关系 貌似会导致越下面 权限越大。</li></ol></td></tr><tr><td>maxkb</td><td><p>系统定义了三级角色层次 permission_constants.py:662-696 ：</p><p>ADMIN: 系统超级管理员，拥有所有权限<br />WORKSPACE_MANAGE: 工作空间管理员，管理工作空间内的资源<br />USER: 普通用户，具有有限的资源访问权限</p></td><td><p>权限系统基于 PermissionConstants 类定义，包含权限组、操作类型和具体权限的组合 permission_constants.py:137-194 ：</p><p>权限组 (Group): KNOWLEDGE, KNOWLEDGE_FOLDER, KNOWLEDGE_DOCUMENT, KNOWLEDGE_WORKFLOW 等<br />操作类型 (Operate): READ, EDIT, CREATE, DELETE, SYNC, VECTOR, GENERATE, EXPORT 等</p><p>权限组合: 如 KNOWLEDGE_READ, KNOWLEDGE_EDIT, KNOWLEDGE_DELETE 等</p><p><br /></p><p><br /></p></td><td><p>树形</p><p><br /></p><p>文件夹是独立的资源</p></td><td><p>知识库页面展示有左侧树，比较清晰</p></td><td><p>用户没有层级</p></td></tr><tr><td><br /></td><td><br /></td><td><br /></td><td><br /></td><td><br /></td><td><br /></td></tr></tbody></table><p><br /></p><h1>需求分析</h1><p>文件/文件夹 权限管理， 需求来源：同一个文件，有些人能看，有些人能改，有些人不能查看</p><p><strong>知识库检索的权限不修改</strong></p><p><br /></p><p><ac:image><ri:attachment ri:filename="image-2026-4-17_15-3-4.png" /></ac:image></p><p>增加字段【生效范围】，取值：对所有子集资源生效/仅对当前资源生效，默认设置为 对所有子集资源生效，表示 子资源是否继承权限 ，使用范围：dataset（所有）、collection（folder类型）</p><p>权限配置 执行逻辑：1. 取消自身的继承字段 2. 更新自身权限配置 3. 如果【仅对当前资源生效】，取消所有子资源的继承（未设置独立权限的子资源） 4. 获取父级权限，如果没有权限，则自动增加父集的 【仅对当前资源的】 读权限（待补充）</p><p>collection获取父级权限逻辑：parentId、datasetId</p><p>关联创建资源逻辑：1. 获取父集权限，设置是 【对所有子级资源生效】，则设置继承为true，否则设置为false</p><p>权限校验逻辑：待补充</p><p><br /></p><h1>fastgpt实现备注</h1><h2>权限配置</h2><ol><li>为避免递归太长影响性能，所有folder都会配置全量clb(Collaborator,数据表为resource_permissions)</li><li>配置权限时，接口下发全量clbs列表，判断实际生效的配置（oldClb.permission ^ newClb.permission并取低位的1，表示权限高的配置），是否与父冲突；<br />如果是folder，则将全量clbs下发到非继承态的子folder；<br />如果冲突了，则设置为非继承态；如果是folder或者冲突了，则下发全量clbs<br />等同于：folder及其子folder按照最新的全量配置；冲突了的资源第一次全量配置，后面增量更新</li><li>判断冲突：1）获取变化了的位 oldClb.permission ^ newClb.permission 2）取变化位的最低位 3）(parent &amp;&amp; ((changedClb.changedRole &amp; parent.permission) !== 0 || changedClb.deleted))<br />父 读 100 子 写 010 无冲突<br />父 写 010 子 读 100 有冲突<br />父 写 010 子 写 010 无变化，无冲突<br />等同于：权限变大，无冲突，仍然继承；权限变小，有冲突，取消继承<br />继承态下，父的owner权限，变更为子的manage权限</li></ol><p>权限校验  输入resourceId、tmbId</p><ol><li>folderPer：继承态且有parent的，parent的权限 ，否则为0 </li><li>myPer：获取自身的权限 ，否则为0</li><li>sumPer(folderPer, myPer) </li><li>sumper(self, group, org) </li></ol><h2>权限校验</h2><p>resourceType：</p><ac:structured-macro ac:name="code" ac:schema-version="1" ac:macro-id="0b2cbe56-c496-495e-8bfb-ec67bf275e65"><ac:plain-text-body><![CDATA[  team = 'team',
  app = 'app',
  dataset = 'dataset',
  collection = 'collection',
  evaluation = 'evaluation',
  model = 'model',
  agentSkill = 'agentSkill'
其中app、 agentSkill 、dataset、collection逻辑相似度高，value的设计和鉴权基本复用；其他的基本没关系]]></ac:plain-text-body></ac:structured-macro><p><strong>4.13 新增 团队管理员支持分配模型权限  和知识库实现关联不大，测试覆盖一下即可</strong></p><p><br /></p><p>read 0b100/4、write 0b010/2、manage 0b001/1（可配置权限，例如move和授权操作）Owner 32位1/4294967295</p><p>仅对话权限 0b100/4、查看对话权限 0b1000/8</p><p>team Owner(在team_member表中)  对所有资源都是owner权限</p><p><br /></p><p>create collection  ：dataset write 以上权限</p><p>update collection ：collection write 以上权限，move操作需要 source和dest folder的manage权限</p><p>detail / list collection ：collection read 以上权限</p><p>delete：dataset/app 需要 owner 权限，collection 需要写权限</p><p>changeOwner: 需要owner权限</p><p><br />dataset的子资源：1）type=folder，parentId为当前id的dataset 2）type!=folder, 所有collection<br />collection的子资源：1）type=folder，parentId为当前id的collection</p><h1>API 逻辑变更</h1><p>原逻辑变更：</p><p>1 只要修改了配置，与父集<strong>冲突</strong> 就改为 非继承态，并配置所有clbs，同步到继承态的所有子folder</p><p>2 move之后，选择 是恢复继承态，设置inheritPermission，删除 除所有者以外的 已有配置，如果是folder则 同步父集并下发到继承态的子集；选择 保持独立配置，则无需修改</p><p>3 恢复继承之后，恢复继承态，设置 inheritPermission，删除 除所有者以外的 已有配置，folder同步父集并下发到继承态的子集</p><p><strong>检查冲突逻辑变更为</strong>：1）父所有者在子为manage 或 owner，否则为冲突 2）子的所有者，在父为write 或 manage 或 owner，否则为冲突 3）其余的只要不同就是冲突，就取消继承</p><p>继承态的非folder资源 只有一个所有者的 clbs配置</p><p>增加一个升级接口，从root开始根据新的逻辑重新配置所有权限</p><p><strong>/api/proApi/core/dataset/collaborator/update  </strong><br />配置权限，原逻辑：<br />1.folder-&gt;配置<strong>所有权限</strong>，同步到继承态子目录协作者<br />2.继承态 -&gt;有冲突，则配置所有权限，改为非继承；无冲突则增量配置到资源<br />3.非继承态→增量更新权限</p><p>修改逻辑： 增加参数 权限范围</p><p>1 folder→范围是所有 配置<strong>所有权限</strong>，同步到继承态子目录协作者，范围是当前，只配置所有权限 <br />2.继承态 →有冲突，则配置所有权限，改为非继承；无冲突则无需处理<br />3.非继承态→增量更新权限<br /><br />增加逻辑：<br />1.当前→所有，1）有独立clbs的非folder子资源，inheritPermission改为true （原独立仍独立）；folder资源的clbs与当前相同的，inheritPermission改为true<br />2.所有→当前，1）所有子资源，inheritPermission改为false<br />3.同步权限到 datasetId &amp;&amp; type: folder 的collection</p><p>风险一：太多资源，递归层数太多——测试10层dataset+10层collection</p><p><strong>/api/core/dataset/update </strong><br />move原逻辑：单独配置仍保留<br />1.原和目的权限校验<br />2.inheritPermission=true<br />3.获取目的端父权限，syncCollaborators同步<strong>父权限</strong>到当前 （当前逻辑，无论folder还是普通dataset都配置了所有权限），并保留已配置的权限，syncChildrenPermission同步<strong>父权限</strong>到子集，删除没有的权限（<strong>当前逻辑BUG</strong>: ff1/ff2 有权限 u1 u2 u3 , move到f1下，f1 有权限u1，move之后，ff1仍然有权限 u1 u2 u3 ,ff2 只有权限 u1）+自身权限</p><p>修改逻辑：增加参数，是否继承新父集的权限，默认 是<br />2 如果是继承新父集的权限，则inheritPermission=true，否则 inheritPermission=false<br />3.如果是继承新父集的权限，则获取目的端父权限，删除自己的clbs，同步<strong>父权限</strong>到当前，同步<strong>父权限</strong>到继承态子集；否则保持独立，无其他操作</p><p><br />增加逻辑：</p><p>1.如果是继承新父集的权限，同步<strong>父权限</strong>到 datasetId &amp;&amp; type: folder 的collection</p><p><br /></p><p><strong>/api/proApi/core/dataset/collection/collaborator/update   新增</strong></p><p>1.配置权限<br />2.范围变更<br />3.<s>遍历collaborators列表，判断 tmb || groupId || orgId 有没有上一级（parantId || datasetId）的读权限，没有的话递归配置父级，到 当前的 读权限（需要配置collection权限可能得场景 1 上一级的权限范围一定是到当前的 2 collection的权限比上一级大，3 collection的权限比上一级小 ，场景2 3 说明 上一级一定有权限，所以可以为上一级增加权限，场景四：用户A没有dataset权限，dataset权限范围到所有，直接给他配置了collection，此时如果增加上一级的权限，会导致权限扩算）故先不处理了  ps.只有collection加了这个逻辑，dataset没加</s></p><p><br /></p><p><strong>/api/core/dataset/collection/update</strong></p><p>增加move权限逻辑</p><p><br /></p><p><strong><span style="color: rgb(31,31,31);">/api/core/dataset/collection/create || </span>上传文件</strong></p><p>增加逻辑：<br />1.如果是folder则获取父集权限(父集Owner变更为manage)+自己owner权限，进行全量配置<br />2.如果父集配置的权限范围是当前，则设置继承为false</p><p>权限校验修改：parent collection write || dataset write</p><p><br /></p><p><strong>/api/core/dataset/resumeInheritPermission 恢复继承权限</strong></p><p>原逻辑：原来只有folder有这个按钮<br />1.如果有parent，如果是foler，把parent+自身的cls <strong>merge得到的权限</strong> 同步到自身，并传递到所有inheritPermission是true的folder dataset；设置inheritPermission: true<br />2.如果没有parent，直接设置inheritPermission: true<br /><br />修改逻辑：<br />1.如果有parent，如果是foler，把parent <strong>权限</strong> 同步到自身，并传递到所有inheritPermission是true的folder dataset；设置inheritPermission: true</p><p>增加逻辑：<br />1.获取 所有inheritPermission是true，datasetid 的folder collection，进行恢复权限<br /><br /><strong>/api/core/dataset/collection/resumeInheritPermission 新增</strong><br />1.获取父类权限，parentId || datasetId<br />2. 如果是folder，则将<strong>父类权限</strong> 同步到自身，并传递到所有inheritPermission是true的folder collection；设置inheritPermission: true<br />3.else，直接设置inheritPermission: true</p><p><br /></p><p>collection 增删改查 修改权限判断逻辑</p><p>data 增删改查 修改权限逻辑</p><p><br /></p><p><strong><span style="color: rgb(31,31,31);">/api/core/dataset/list</span></strong></p><p>原逻辑：1）获取 tmbId 资源类型为dataset的全部clbs 2）查询dataset $or: [idList, parseParentIdInMongo(parentId)] 3）遍历datasets列表，获取每一个dataset的权限，如果是继承态则 使用parentId的权限，否则使用第一步获取的4）过滤掉没有读权限的dataset<br />parentId 相关代码自相矛盾，看不懂</p><p>当search的时候 parentId 无效</p><p><br /></p><p><strong><span style="color: rgb(31,31,31);">/api/core/dataset/collection/listV2</span></strong></p><p><span style="color: rgb(31,31,31);">原逻辑：校验dataset读权限<br />修改为：如果有parentId，则校验parentId读权限，否则校验dataset读权限<br />列表实现逻辑：<br />1）获取 tmbId 资源类型为collection 的全部clbs 2）获取 datasetid parentId 和 sql可支持过滤的所有 collections 3）遍历列表，获取每一个权限 4）过滤<br /></span></p><p><span style="color: rgb(31,31,31);">风险：如果上传了上千或上万个文件，性能？</span></p><p><br /></p><p><strong><span style="color: rgb(31,31,31);">/api/proApi/core/dataset/changeOwner</span></strong></p><p><span style="color: rgb(31,31,31);">原逻辑<br />1 获取资源列表，获取该资源及其所有子资源<br />2 更新资源所有者，并且<strong>本资源取消继承，子资源仅修改所有者</strong>（old-&gt;new）<br />3 同步更新关联的外链/OpenAPI 表<br />4 更新权限表（resource_permission）<br /></span></p><p>处理新旧所有者在协作权限表中的冲突，分三种情况：</p><table class="wrapped"><thead class=""><tr class=""><th>情况</th><th>处理逻辑</th></tr></thead><tbody class=""><tr class=""><td>新旧所有者都有权限</td><td>取两者权限的<strong>最大值</strong>，删除新所有者的旧记录，将旧所有者的记录更新为新所有者 + 最大权限</td></tr><tr class=""><td>只有旧所有者有权限</td><td>直接将旧所有者的记录更新为新所有者</td></tr><tr class=""><td>只有新所有者有权限</td><td>不做任何处理</td></tr></tbody></table><p><span style="color: rgb(31,31,31);"> </span></p><p><span style="color: rgb(31,31,31);">修改权限：</span></p><p><span style="color: rgb(31,31,31);">1 dataset 对collection 变更 所有者</span></p><p><span style="color: rgb(31,31,31);"><strong>/api/proApi/core/dataset/collection/changeOwner 新增 转移所有权</strong></span></p><p><br /></p><p><span style="color: rgb(31,31,31);">团队转移owner：入口条件：当前用户是团队的 owner（userInfo?.team.permission.isOwner），且团队是企业微信团队（isWecomTeam 为 true）</span></p><p><br /></p><p><span style="color: rgb(31,31,31);">关联修改app接口<br />/api/core/app/update</span></p><p><span style="color: rgb(31,31,31);">/api/core/app/folder/create</span></p><p><span style="color: rgb(31,31,31);">/api/proApi/core/app/collaborator/update</span></p><p><span style="color: rgb(31,31,31);">/api/core/app/create</span></p><h1>Dataset/Collection 权限继承逻辑重构计划</h1><h2>Context</h2><p>当前 FastGPT 的 dataset（知识库）和 collection（文件/文件夹）权限继承系统存在以下问题：</p><ol><li><strong>Move 后权限残留 Bug</strong>：dataset 或 collection 移动到新父目录后，原有独立权限未被清理，导致子资源仍保留旧权限。</li><li><strong>继承态非 folder 资源仍保留 clbs</strong>：按设计，继承态的非 folder 资源不应有任何 clb 配置，但当前逻辑未强制清理。</li><li><strong>冲突检测逻辑不完善</strong>：原逻辑仅检测&quot;子集冲突&quot;，需改为检测&quot;不同即冲突&quot;。</li><li><strong>恢复继承时错误 merge 自身权限</strong>：<code>resumeInheritPermission</code><span> </span>原逻辑将 parent + 自身 clbs merge 后同步，应改为仅同步 parent 权限。</li><li><strong>Collection 权限体系缺失</strong>：collection 层级的协作者配置、继承恢复、移动同步等逻辑不完整。</li><li><strong>List 接口权限计算矛盾</strong>：<code>dataset/list</code><span> </span>中 parentId 相关代码逻辑不一致，search 时 parentId 失效导致权限过滤异常。</li></ol><p>本次重构目标：统一 dataset 和 collection 的权限继承语义，确保：</p><ul><li><strong>继承态资源</strong>（<code>inheritPermission=true</code>）：非 folder 资源无任何 clb；folder 资源的 clb 与父级完全一致。</li><li><strong>非继承态资源</strong>（<code>inheritPermission=false</code>）：拥有独立的 clb 配置。</li><li><strong>Move/恢复继承/配置变更</strong>时，权限同步逻辑清晰、无残留。</li></ul><hr /><h2>关键概念</h2><table class="wrapped"><thead class=""><tr class=""><th style="text-align: left;">术语</th><th style="text-align: left;">含义</th></tr></thead><tbody class=""><tr class=""><td style="text-align: left;"><code>clbs</code></td><td style="text-align: left;"><code>MongoResourcePermission</code><span> </span>中的协作者记录（tmbId/groupId/orgId + permission）</td></tr><tr class=""><td style="text-align: left;"><code>inheritPermission</code></td><td style="text-align: left;"><code>true</code><span> </span>表示继承父级权限；<code>false</code><span> </span>表示独立权限</td></tr><tr class=""><td style="text-align: left;"><code>permissionEffectScope</code></td><td style="text-align: left;"><code>allChildren</code>（默认，权限影响子资源） /<span> </span><code>currentOnly</code>（仅影响当前）</td></tr><tr class=""><td style="text-align: left;"><code>folder</code></td><td style="text-align: left;"><code>DatasetTypeEnum.folder</code>（dataset 层级）或<span> </span><code>DatasetCollectionTypeEnum.folder</code>（collection 层级）</td></tr><tr class=""><td style="text-align: left;"><code>syncCollaborators()</code></td><td style="text-align: left;">将父级 clbs 合并到目标资源（增量：父有己无则加，父无己有且与父旧值相同则删）</td></tr><tr class=""><td style="text-align: left;"><code>syncChildrenPermission()</code></td><td style="text-align: left;">BFS 遍历所有<span> </span><code>inheritPermission=true</code><span> </span>的 folder 子资源，同步 clbs</td></tr><tr class=""><td style="text-align: left;"><code>resumeInheritPermission()</code></td><td style="text-align: left;">恢复继承态：folder 同步父级权限到自身及子资源；非 folder 仅设<span> </span><code>inheritPermission=true</code></td></tr></tbody></table><p><br /></p><p><br /></p><h1>测试分析</h1><h2>正交维度说明</h2><table class="wrapped"><thead class=""><tr class=""><th>维度</th><th>取值</th></tr></thead><tbody class=""><tr class=""><td><strong>Collection 类型</strong></td><td><code>folder</code><span> </span>/<span> </span><code>file</code></td></tr><tr class=""><td><strong>inheritPermission</strong></td><td><code>true</code>（继承）/<span> </span><code>false</code>（独立）</td></tr><tr class=""><td><strong>父级类型</strong></td><td><code>dataset</code>（根级）/<span> </span><code>folder</code>（嵌套）</td></tr><tr class=""><td><strong>请求权限（per）</strong></td><td><code>ReadPermissionVal</code>（读） /<span> </span><code>WritePermissionVal</code>（写） /<span> </span><code>ManagePermissionVal</code>（管理）</td></tr></tbody></table><p><br /></p><h2>核心测试案例表</h2><p><br /></p><p><br /></p><table class="wrapped" style="color: rgb(23,43,77);font-size: 14.0px;font-weight: 400;letter-spacing: 0.0px;"><thead class=""><tr class=""><th><br /></th><th><br /></th><th><br /></th></tr></thead><tbody class=""><tr class=""><td><br /></td><td><br /></td><td><br /></td></tr><tr class=""><td><br /></td><td><br /></td><td><br /></td></tr><tr class=""><td><br /></td><td><br /></td><td><br /></td></tr></tbody></table><p><br /></p>
